language: ruby
rvm:
        - 2.0.0
addons:
        postgresql: "9.3"
before_script:
        - psql -c 'create database travis_ci_test;' -U postgres
        - cp config/database.yml.travis config/database.yml
        - cp config/vizard.yml.travis config/vizard.yml
notifications:
    email:
        recipients:
            - lab@ciudadanointeligente.org
        on_success: change
        on_failure: always
