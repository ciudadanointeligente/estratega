<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Vizard Share</title>
</head>
<body>
  <p><%= t('.new_add_to_share') %></p>

  <p><%= link_to @project.title, project_url(@project) %></p>

  <% if @message %>
    <p><%= @message %></p>
  <% end %>

  <%= link_to t('.activate_account'), edit_password_url(@user, reset_password_token: @token, subdomain: Apartment::Tenant.current) %>
</body>
</html>