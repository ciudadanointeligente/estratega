<%= simple_form_for [@activity, @ask] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :description %>
    <%# f.association :activity %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
