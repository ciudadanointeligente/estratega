<%= simple_form_for [@objective, @outcome] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :description %>
    <%# f.association :objective %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
